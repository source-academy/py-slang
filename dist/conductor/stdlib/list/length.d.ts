import { IDataHandler, List } from "../../types";
/**
 * Gets the length of a List.
 * @param xs The List to get the length of.
 * @returns The length of the List.
 */
export declare function length(this: IDataHandler, xs: List): number;
