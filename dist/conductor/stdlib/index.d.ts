import { accumulate, is_list, length } from "./list";
export declare const stdlib: {
    is_list: typeof is_list;
    accumulate: typeof accumulate;
    length: typeof length;
};
export { accumulate };
